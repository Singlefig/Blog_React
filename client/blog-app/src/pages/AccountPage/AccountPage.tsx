import React from "react";
import { connect } from "react-redux";

const AccountPage = () => {
    return (
        <div>
            Account
        </div>
    );
};

export default connect()(AccountPage);